i64 MemCh_Print(struct stream *sm, Abstract *a, cls type, boolean extended);
i64 MemBook_Print(struct stream *sm, Abstract *a, cls type, boolean extended);
i64 Span_Print(struct stream *sm, Abstract *a, cls type, boolean extended);
status Mem_DebugInit(MemCh *m, struct lookup *lk);
