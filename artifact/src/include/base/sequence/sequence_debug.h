i64 Iter_Print(Stream *sm, Abstract *a, cls type, boolean extended);
i64 Slab_Print(Stream *sm, slab *slab, i8 dim, i8 dims);
i64 Span_Print(Stream *sm, Abstract *a, cls type, boolean extended);
i64 Lookup_Print(Stream *sm, Abstract *a, cls type, boolean extended);
i64 HKey_Print(Stream *sm, Abstract *a, cls type, boolean extended);
status SequenceDebug_Init(MemCh *m, struct lookup *lk);
