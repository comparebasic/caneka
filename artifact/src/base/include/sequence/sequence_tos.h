i64 Slab_Print(Stream *sm, slab *slab, i8 dim, i8 dims);
i64 Iter_Print(Stream *sm, Abstract *a, cls type, word flags);
i64 Span_Print(Stream *sm, Abstract *a, cls type, word flags);
i64 Lookup_Print(Stream *sm, Abstract *a, cls type, word flags);
i64 HKey_Print(Stream *sm, Abstract *a, cls type, word flags);
status Sequence_ToSInit(MemCh *m, struct lookup *lk);
