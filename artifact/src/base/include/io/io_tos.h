status ProcDets_Print(Buff *bf, Abstract *a, cls type, word flags);
status StashDebug_Init(MemCh *m, struct lookup *lk);
status StashCoords_Print(Buff *bf, StashCoord *coord, word flags);
char *Buff_WhenceChars(i64 whence);
