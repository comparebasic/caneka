#include <external.h>
#include <caneka.h>

static Lookup *fmtToHtmlLookup = NULL;

status Fmt_ToHtml(Stream *sm, Mess *mess){
    
    return SUCCESS;
}

status FmtToHtml_Init(){
    if(fmtToHtmlLookup == NULL){

    }
}
