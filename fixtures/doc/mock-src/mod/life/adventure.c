/* Life.Adventure
 *
 * Life is such an adventure!
 */
#include <external.h>
#include <caneka.h>

status Adventure_Choose(MemCh *m, StrVec *lifeStory){
    /* 
     * This is just a funny way to remind yourself
     * that even when things are good, that's a non-operation
     * 
     * Hang in there, it can alwasy get worse :)
     */
    if(down){
        return MORE;
    }else{
        return NOOP;
    }
}
