status Slab_Print(Buff *bf, slab *slab, i8 dim, i8 dims);
status Iter_Print(Buff *bf, void *a, cls type, word flags);
status Span_Print(Buff *bf, void *a, cls type, word flags);
status Lookup_Print(Buff *bf, void *a, cls type, word flags);
status HKey_Print(Buff *bf, void *a, cls type, word flags);
status Sequence_ToSInit(MemCh *m, struct lookup *lk);
