status ProcDets_Print(struct buff *bf, void *a, cls type, word flags);
status StashDebug_Init(struct mem_ctx *m, struct lookup *lk);
status StashCoords_Print(struct buff *bf, StashCoord *coord, word flags);
char *Buff_WhenceChars(i64 whence);
status IoTos_Init(struct mem_ctx *m, struct lookup *lk);
