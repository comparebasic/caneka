The code in this folder is under the same licence as the rest of Caneka.

See the 3-clause BSD licence from Compare Basic at ../../LICENCE.

The subsequent folder names do not contain the sources of the libraries that
bear the names of the folders. For example OpenSSL is not he OpenSSL library,
it is bridge code between Caneka and those respective libraries.
