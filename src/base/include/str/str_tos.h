status Str_Print(struct buff *bf, Abstract *a, cls type, word flags);
status StrVec_Print(struct buff *bf, Abstract *a, cls type, word flags);
status Str_TosInit(MemCh *m, struct lookup *lk);
status Bytes_Debug(struct buff *bf, byte *start, byte *end);
