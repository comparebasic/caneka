#Require Caneka

#Type Cash {
    Make(MemCtx m, Getter get, Abs source) -> Cash {
        this.m = m;
        this.rbl = /between({^$.},$ -> value),value({_\{^\}.} -> between)/
        this.get = get;
        this.source = source;
        this
    }
}
